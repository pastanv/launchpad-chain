// The `handlers/` directory is for organizing request handler functions.
// Handler functions should only implement request handling logic. Any resource-related functions,
// such as CRUD operations in the database should be handled by an external function from `services/` directory.
